export class Thematique {

      id: number;
      nom: string;
      description: string;
      niveau: number;
      idParent: number;
      imagePath: string;

      constructor() { }

/*
      setNom(nom: string) {
          console.log(nom);
          this.nom = nom;
      }

      setDescription(description: string) {
          this.description = description;
      }

      setNiveau(niveau: number) {
          this.niveau = niveau;
      }

      setIdParent(idParent: number) {
          this.idParent = idParent;
      }

      getNom(): string {
          return this.nom;
      }

      getDescription(): string {
          return this.description;
      }

      getNiveau(): number {
          return this.niveau;
      }

      getIdParent(): number {
          return this.idParent;
      }
*/

}
